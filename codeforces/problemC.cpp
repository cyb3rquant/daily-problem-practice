//




#include <bits/stdc++.h>
using namespace std;
#define ln '\n'
#define forn(i, n) for (int i = 0; i < n; i++)
#define pb push_back
typedef long long ll;
typedef vector<int> vecin;
typedef vector<ll> vecll;




int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int t;
    cin >> t;
    while (t > 0)
    {
        ll l,r;
        cin>>l>>r;



//----Prime-Check----
        bool isPrime = true;
        for (int i = 2; i <= sqrt(d); i++)
        {
            if (d % i == 0)
                isPrime = false;
        }

        // if(isPrime) cout<<"PRIME"<<ln;
        // else cout<<"NOT PRIME"<<ln;//---counting the number of digits in n---
    int digicheck = n;
    int c = 0;
    while (digicheck > 0)
    {
        digicheck /= 10;
        c++;

    }
    
    //---generate 1 to n integers---
vecin intlist1toN;
forn(i, n)
{
    intlist1toN[i] = i + 1;
}




        t--;
    }
}